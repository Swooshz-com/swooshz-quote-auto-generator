# Historical runtime privilege contract fragment

This is the bounded documentation fragment present before the membership
query narrative hardening. It intentionally has no `Membership-query narrative
contract` section; the preserved retrospective test must therefore fail its
documentation assertion.

The runtime/provider edge is represented as a single PostgreSQL 17 creator
administration edge. Broader protected-role membership closure was not yet
described by this historical fragment.
